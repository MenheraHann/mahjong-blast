# 乐信圣文麻将游戏 - 开发迭代记录

## 2026-04-05

### 初始需求
- 阅读测试题PDF，明确需求：开发一款类Vita Mahjong的麻将手游
- 核心要求：首页、游戏页、结果页，支持至少20关，手机可玩，24小时完成

### 方案选型
- **对比Unity vs Web方案**，最终选择Web方案（HTML5 + Phaser.js）
- 理由：开发效率高、即时预览、无需构建、Vercel一键部署
- 数据持久化：使用localStorage保存关卡进度

### 第一版代码（失败）
- 创建了基础文件结构：`index.html`、`js/game.js`、`js/scenes/home.js`、`js/scenes/game.js`、`js/scenes/results.js`
- 配置PWA：`manifest.json`、`service-worker.js`
- **问题**：页面黑屏，无任何内容显示
- **原因**：JavaScript加载顺序错误，场景类在game.js之后加载，导致`HomeScene`未定义

### 第二版代码（部分修复）
- 调整index.html中script标签顺序，场景文件先于game.js加载
- 首页成功显示（标题+开始按钮+底部说明）
- **问题**：点击"开始游戏"无反应
- **原因**：`createBoard()`方法中使用了`h`变量，但`h`只在`create()`中定义为局部变量，`createBoard()`无法访问

### 第三版代码（当前）
- 将`w`和`h`改为`this.w`和`this.h`，挂载到场景实例上
- 修复场景构造函数写法：`super({ key: 'GameScene' })`
- 游戏场景能正常进入，显示麻将牌面
- **已验证**：配对逻辑、过关流程、结果页跳转正常

### 顶部信息栏开发
- **用户需求**：
  > 上方显示，有五个要显示的内容，从左到右分别是
  > 1. 返回按键，点击后二次询问，确定后返回主页面
  > 2. 当前关卡数，当前的第几关
  > 3. 分数，完成一次二销加200分
  > 4. p匹配：当前厂商可以匹配的对数
  > 5. 菜单栏，点击后显示菜单，内容包括重新开始和返回主菜单
- 参考Vita Mahjong截图，新增顶部信息栏，包含五个元素：
  1. **返回按钮**（←）— 点击弹出二次确认弹窗，确定后返回主页面
  2. **关卡数** — 显示当前第几关
  3. **分数** — 每次配对成功 +200 分，黄色显示
  4. **匹配** — 显示当前剩余可匹配对数，配对成功后动态减1，绿色显示
  5. **菜单按钮**（≡）— 点击弹出菜单，包含「重新开始」和「返回主菜单」
- 弹窗组件：`showConfirmDialog()` 和 `showMenu()`，使用遮罩层 + 深度控制

### 牌面布局调整
- **用户需求**：
  > 现在修改一下排面，我要求这个排面布局是7*8的，7是宽，8是高
- 固定布局为 7列×8行 = 56张牌（28对）
- 牌尺寸从70px调整为60px，间距从8px调整为6px以适配屏幕

### 底部功能栏开发
- **用户需求**：
  > 好的，在页面下方添加两个功能，分别是提示功能和重新洗牌，
  >
  > 1. 提示功能点击之后，就会高亮一对匹配的牌，持续高亮的状态，等待用户去点击，点击之后才会消失，用户默认有10次提示的机会，没次数的时候，图标就暗下去了，图标用灯泡的图标
  >
  > 2. 重新洗牌就是整个牌桌上当前牌的种类和数量不变，位置就行一次大调换，完全随机摆放
- 页面底部新增两个功能按钮：
  1. **重新洗牌**（🔀）— 保持当前未消除牌的种类和数量不变，只随机重排位置与牌面内容
  2. **提示**（💡）— 默认10次，点击后高亮一对可匹配牌，直到用户点击提示中的牌后才取消高亮
- 提示次数为0时，灯泡图标自动变暗并禁用点击
- 洗牌后会自动清除当前提示高亮，避免状态混乱

### 提示高亮逻辑调整
- **用户需求**：
  > 添加一条，提示的高亮不要消失，除非用户把它消掉了，不然它会一直存在
- 调整提示逻辑：点击普通牌时不再清除提示高亮
- 只有当用户真正把被提示的那一对牌匹配消除后，提示高亮才会消失

### Emoji替换为SVG图标
- **用户需求**：
  > 不要使用emoji，把所有的emoji都替换为svg图标
- 新建 `assets/icons/` 目录，创建6个SVG矢量图标：
  - `back.svg` — 返回箭头
  - `menu.svg` — 三横线菜单
  - `shuffle.svg` — 交叉箭头（洗牌）
  - `hint.svg` — 灯泡（提示）
  - `trophy.svg` — 奖杯（过关成功）
  - `fail.svg` — 叉号（过关失败）
- GameScene 通过 `this.load.svg()` 加载图标，替换原有的emoji文字按钮
- ResultsScene 同步替换成功/失败图标

### 麻将牌面替换为真实麻将图片
- **用户需求**：
  > 我在这个文件夹里放了34张png，数字从1-34，分别是一万到九万(1-9)、一到九条(10-18)、一到九筒(19-27)、东南西北中发白(28-34)，你替换一下
- preload中批量加载 `assets/Majong/tablemjh1.png` ~ `tablemjh34.png`
- createBoard中用 `this.add.image()` 替换原有的emoji文字，图片缩放至 `tileSize - 8` 适配牌面
- shuffleBoard中同步更新图片引用（`tileImg.setTexture()`）

### 布局居中与显示优化
- Phaser 画布改为 720×1280 固定设计分辨率 + FIT 等比缩放
- 禁止页面滚动和缩放（`position: fixed` + `minimum-scale=1.0`）
- 牌面动态计算尺寸，适配可用区域居中显示

### 麻将贴图替换为 pixelMajong
- **用户需求**：
  > 牌用的贴图替换为 /assets/pixelMajong 文件夹里面的，一万到九万01~09、一饼到九饼11~19、妖姬到9条31~39、北西南东发中白板41~47
- 原始PNG尺寸 5097×7038，使用 Pillow 批量缩小至 100×138（保持比例）
- 总大小从 ~100MB 降至 240KB
- 更新加载路径和缩放计算基准

### SVG图标缩小
- **用户需求**：
  > 把所有的svg图标缩小到现在的一半
- 返回按钮：0.8 → 0.4
- 菜单按钮：0.9 → 0.45
- 洗牌/提示按钮：1.2 → 0.6

### 选中/提示边框优化
- **用户需求**：
  > 我希望他是把当前选中的牌的png外边缘描一个边，而不是单独的一个框
- 使用 `this.add.graphics()` + `strokeRoundedRect()` 绘制圆角描边
- 替代原有的矩形背景框方案，边框紧贴PNG边缘
- 选中为红色描边，提示为黄色描边

### 牌面整体缩小90%
- **用户需求**：
  > 这个排面，整体缩小90%
- `tileSize` 计算后乘以 0.9 系数

### 选中效果修改
- **用户需求**：
  > 现在我需要你修改一下选中的效果 1. 被选中的牌需要添加一个浅绿色的图层效果，叠加在牌上面 2. 被选中的牌会被放大10%，从牌的左上角为中心放大，同时牌来到最上层
- 选中效果从红色描边改为浅绿色半透明叠加层（`0x90ee90`，透明度0.35）
- 选中牌以左上角为中心放大10%（`setOrigin(0,0)` + `setScale(1.1)`）
- 选中牌置顶显示（`setDepth(10)`）
- 新增 `resetTileState()` 辅助方法，配对成功/失败后恢复牌的原始状态
- 同步更新洗牌逻辑，洗牌时清除残留的选中叠加层

### 选中效果迭代修复
- **选中置顶层级**：`setDepth(10)` 改为 `setDepth(100)`，确保选中牌始终在最上层（最大 defaultDepth 为 55）
- **配对消除改为完全隐藏**：配对成功后用 `setVisible(false)` 替代 `setAlpha(0.3)`，不再显示半透明残影
- **取消选中恢复缩放**：修复取消选中后牌偏大的 bug——创建牌时存储 `imgScale`，取消选中时恢复到原始缩放值而非 `setScale(1)`
- **默认层级恢复**：`clearTileSelection()` 改为从 `tile.getData('defaultDepth')` 恢复层级，而非统一重置为 0
- **取消选中置顶**：`resetTileState()` 移除重复的 `setDepth(0)`，由 `clearTileSelection` 统一处理

### 牌面布局调整
- **间距归零**：去掉 0.9 缩放系数，牌填满格子，横向纵向间距均为 0
- **纵向重叠 -20px**：`y = startY + row * (tileHeight - 20)`
- **横向重叠 -10px**：`x = startX + col * (tileSize - 10)`
- **牌面间距标准**：紧贴排列时，横向间距 -10px，纵向间距 -20px

## 2026-04-06

### 角压覆盖修复
- **问题**：角压牌（corner）的 `coverage` 只包含对角线2个位置，导致2×2网格中未被纳入的牌（如左上角的四万）没有被锁定
- **修复**：角压候选的 `coverage` 改为包含2×2网格全部4个位置 `[[r,c], [r,c+1], [r+1,c], [r+1,c+1]]`
- 过滤阶段移除覆盖四角的候选，但保留所有非四角位置
- 消除第二层牌时遍历全部 `coveredPositions` 解锁所有被覆盖的第一层牌

### 代码清理
- 删除 `createBoard()` 重复定义（残留的不完整方法体）
- 删除 `onTileClick()` 中残留的孤立代码块（原754-896行）
- 最终保留唯一正确的 `createBoard()` 和 `onTileClick()` 定义

### 取消选中 tint 修复
- `clearTileSelection()` 新增 tint 恢复逻辑：自由牌清除 tint，锁定牌恢复灰色 `0x999999`
- 修复取消选中后绿色蒙版不消失的 bug

### 点击逻辑重构
- 牌面上最多只有一张牌处于选中状态
- 选中A → 选中B：若A、B相同则消除；若不同则A取消选中，B变为选中
- 消除时不再有600ms等待，不同类型立即切换选中目标

### 第二层偏移调整
- 第二层相对于第一层：横向偏移 -10px，纵向偏移 -12px（原纵向 -10px）
- 用 `layerOffsetX` / `layerOffsetY` 变量定义，便于未来多层级扩展

### 提示功能改为蒙版效果
- 提示牌从黄色描边框改为黄色蒙版 `setTint(0xf1c40f)`
- 选中提示牌时蒙版变为绿色，取消选中恢复黄色
- 消除/洗牌时清除提示蒙版
- 移除所有 `hintBorder` 相关代码，改用 `hintTint` 标记

### 表现层动画迭代
- **选中动画**：选中和取消选中改为缓入缓出缩放动画，放大倍数调整为 `1.15x`
- **菜单层级修复**：菜单/确认弹窗层级提升到 `5000/5001`，避免被牌面遮挡
- **背景层级修复**：背景固定在最底层 `depth -9999`，保证 `depth -1` 的阴影可见
- **碰撞消除动画**：新增二销动画，流程为「起飞 → 靠近接触 → 向中心缩小消失」
- **碰撞偏移**：起飞目标调整为碰撞点左右 `±200px`
- **消除过程去绿**：进入二销动画前清除两张牌的绿色选中蒙版
- **动画时长校准**：基于录屏后逐帧校对，最终确定为：
  - 起飞：`330ms`
  - 靠近碰撞：`30ms`
  - 缩小消失：`300ms`

### 牌面配对保证修复
- **问题**：牌面类型随机分配，可能出现奇数张同类型牌，导致无法全部消除
- **修复**：所有牌面类型分配改为"先生成成对类型再打乱"
  - 第一层56张：按 `0,0,1,1,2,2...33,33` 循环生成后打乱
  - 第二层8-12张：同样按成对方式生成
  - 手动洗牌（`getShuffledTypesForLayer2`）：重新生成成对类型，不再只是打乱现有类型
  - 智能洗牌（`smartShuffle`）：同上，保证洗牌后每种类型仍成对
- **效果**：保证每张牌都有配对，一定能通过二销清除所有牌

### 入场动画系统
- **7种入场动画**，每次进入关卡随机播放一种：
  1. 左右交替飞入（按列奇偶从左/右飞入，按行从上到下延迟）
  2. 从上到下飞入（第0行先出现，逐行向下）
  3. 从下到上飞入（第7行先出现，逐行向上）
  4. 从右下角弹性放大
  5. 从左下角弹性放大
  6. 从右上角弹性放大
  7. 从左上角弹性放大
- **统一时长**：所有动画总时长约 1000ms
- **弹性效果**：手动两段式弹性（过冲→弹回），幅度减半
  - 飞入：过冲 15px + 80ms 弹回
  - 放大：过冲 1.07x + 80ms 弹回
- **锁定状态**：动画开始前即显示所有锁定牌的灰色蒙版
- **阴影渐显**：牌入场完成后，阴影 300ms 渐显
- **动画期间禁止点击**，结束后恢复交互并初始化自由状态

### 待完成
- [ ] 部署到Vercel

### 暗扣牌机制
- **功能**：每关随机将总牌数的 1/6 ~ 1/3 设为暗扣牌（背面 00.png 显示），保证偶数张以确保可配对消除
- **选中翻转**：点击暗扣牌执行水平翻转动画（shrink→换正面图→grow），阴影同步翻转，翻正后显示绿色选中效果
- **取消翻转**：再次点击执行反向翻转回背面，清除绿色 tint
- **消除动画**：暗扣牌在消除动画（起飞→靠近→缩小消失）期间保持正面状态，不再翻转
- **洗牌保持**：洗牌时暗扣牌保持背面显示，仅内部更新牌型数据
- **`resetTileState`** 中暗扣牌恢复背面
- **代码重构**：`createBoard()` 拆分为 `createBoard()` + `generateRandomLayout()`，为后续自定义关卡预留扩展点

### 提示功能增强：暗扣牌永久翻正 + 钟摆摇晃
- **提示翻正**：使用提示时，若匹配的牌是暗扣牌，执行水平翻转动画永久翻正为正面，作为普通牌参与游戏
- **黄色保持**：提示牌的黄色在各种场景下保持不消失（`recalculateAllFreeStatus`、消除回调、选中动画中均加 `hintTint` 检查）
- **钟摆摇晃**：提示牌每 1200ms 执行一次弹性摇晃（三次：小-4°→大±8°→小-4°，共 600ms），阴影同步跟随
- **选中不打断**：选中提示牌时变绿+放大，摇晃持续不中断；取消选中恢复黄色+继续摇晃
- **消除停止**：进入匹配消除动画时停止摇晃
